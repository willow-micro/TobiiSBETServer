<Window x:Class="TobiiSBETServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TobiiSBETServer"
        mc:Ignorable="d"
        Title="Tobii Screen-based Eye Tracker Server" SizeToContent="WidthAndHeight">
    <Window.InputBindings>
        <KeyBinding Command="ApplicationCommands.Close" Key="Esc" />
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="AppCloseEvent" />
    </Window.CommandBindings>
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <Label x:Name="statusLabel" VerticalAlignment="Center"/>
        </StatusBar>
        <Grid x:Name="mainGrid" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
        </Grid>
    </DockPanel>
</Window>
