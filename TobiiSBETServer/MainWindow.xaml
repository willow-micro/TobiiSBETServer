<Window x:Class="TobiiSBETServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TobiiSBETServer"
        mc:Ignorable="d"
        Title="Tobii Screen-based Eye Tracker Server" SizeToContent="WidthAndHeight">
    <Window.Resources>
        <Style x:Key="GroupBoxStyle" TargetType="GroupBox">
            <Setter Property="Margin" Value="6" />
        </Style>
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Command="ApplicationCommands.Close" Key="Esc" />
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="AppCloseEvent" />
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StatusBar Grid.Row="3">
            <Label x:Name="statusLabel" VerticalAlignment="Center" Content="Default"/>
        </StatusBar>
        <GroupBox Grid.Row="0" Header="Device"
                  Style="{StaticResource GroupBoxStyle}">
            <StackPanel Orientation="Horizontal">
                <Label VerticalContentAlignment="Center" Content="Serial Number:" />
                <Label VerticalContentAlignment="Center" Content="{Binding SerialNumberStr, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Row="1" Header="Data"
                  Style="{StaticResource GroupBoxStyle}">
            <StackPanel Orientation="Horizontal">
                <Label VerticalContentAlignment="Center" Content="Serial Number:" />
                <Label VerticalContentAlignment="Center" Content="{Binding SerialNumberStr, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </GroupBox>
    </Grid>
    <!--
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <Label x:Name="statusLabel" VerticalAlignment="Center"/>
        </StatusBar>
        <Grid x:Name="mainGrid" HorizontalAlignment="Center" VerticalAlignment="Top"
              Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Grid.Column="0" Header="Device">
                <StackPanel Orientation="Horizontal">
                    <Label VerticalContentAlignment="Center" Content="Serial Number:" />
                    <Label VerticalContentAlignment="Center" Content="{Binding SerialNumberStr, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </GroupBox>
        </Grid>
    </DockPanel>
    -->
</Window>
